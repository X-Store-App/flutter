import 'package:flutter/material.dart';

Scaffold Function(BuildContext context) accountPage =
    (BuildContext context) => Scaffold(
          appBar: AppBar(title: Text("Account")),
          body: Column(
            children: [],
          ),
        );
